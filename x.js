// x.js — Datenmodul (bereinigt, EIN ZEICHEN + RAW)

export const X = {
  uni: "X",                     // EIN ZEICHEN (UNI‑gültig)
  raw: "RAW:DATA-SET-4"         // RAW‑Deutung (bereinigter Datensatz)
};

export function getX() {
  return X;
}
