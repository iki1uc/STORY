// y.js — Datenmodul (bereinigt, EIN ZEICHEN + RAW)

export const Y = {
  uni: "Y",                     // EIN ZEICHEN (UNI‑gültig)
  raw: "RAW:DATA-SET-5"         // RAW‑Deutung (bereinigter Datensatz)
};

export function getY() {
  return Y;
}
